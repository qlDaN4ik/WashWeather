package com.example.washweather.data.db.entity

data class Advices(
        var needGlasses: Boolean,
        var needUmbrella: Boolean,
        var needWash: Boolean,
        var needLight: Boolean,
        var needWear: Boolean
)